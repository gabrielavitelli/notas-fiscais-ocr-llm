# OCR (opencv-headless evita libGL em servidor/nuvem; packages.txt adiciona libgl1 se necessário)
opencv-python-headless>=4.8.0
python-doctr[torch]>=0.7.0

# LLM — Groq (grátis) ou Hugging Face (grátis) ou transformers (local)
openai>=1.0.0         # usado para Groq e HF (API compatível)
# transformers torch   # opcional: LLM local (pip install transformers torch)

# Google Sheets
gspread>=6.0.0
google-auth>=2.0.0

# PDF/imagem
Pillow>=10.0.0

# Interface web (local, sem Google Cloud)
streamlit>=1.28.0
pandas>=2.0.0

# Carregar chaves do arquivo .env (configuração fixa)
python-dotenv>=1.0.0
