# Mínimo para o app abrir na nuvem (Streamlit Cloud).
# No PC, para OCR + Sheets: pip install python-doctr[torch] opencv-python-headless gspread google-auth

streamlit>=1.28.0
pandas>=2.0.0
openai>=1.0.0
Pillow>=10.0.0
python-dotenv>=1.0.0
