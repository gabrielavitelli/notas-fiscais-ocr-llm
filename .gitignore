# Chaves e segredos (nunca commitar)
.env

# Documentos pessoais / notas fiscais (não subir ao GitHub)
*.pdf
*.PDF
nf_extraidas*.csv
nf_extraidas2*.csv

# packages.txt: se existir com libgl1, pode quebrar o build em alguns ambientes; remover e push para subir sem ele
packages.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Streamlit
.streamlit/secrets.toml

# Estado persistente do app (memória local)
notas_fiscais_state.json
notas_fiscais_duvidas_erros.json
# Diretório padrão: CSV, estado e log (criado automaticamente)
nf_dados/
